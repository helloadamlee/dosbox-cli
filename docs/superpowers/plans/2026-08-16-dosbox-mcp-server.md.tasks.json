{
  "planPath": "docs/superpowers/plans/2026-08-16-dosbox-mcp-server.md",
  "tasks": [
    {
      "id": 0,
      "subject": "Task 1: Package scaffold and client bridge",
      "status": "pending",
      "description": "**Goal:** Create the `mcp-server/` package skeleton, installable with `pip install -e`, with a working `sys.path` bridge to `scripts/host_control_client.py`.\n\n**Files:**\n- Create: `mcp-server/pyproject.toml`\n- Create: `mcp-server/dosbox_mcp/__init__.py`\n- Create: `mcp-server/dosbox_mcp/_client_import.py`\n- Test: `mcp-server/tests/__init__.py`\n- Test: `mcp-server/tests/test_client_bridge.py`\n\n**Acceptance Criteria:**\n- [ ] `pip install -e mcp-server/[test]` succeeds from the repo root.\n- [ ] `dosbox_mcp._client_import` exposes `PipeTransport`, `SessionClosed`, `RequestTimeout`, `encode_request`, `read_event_line`, `make_deadline`, `remaining_seconds`, all identical objects to the ones in `scripts/host_control_client.py` (same module, not a copy).\n\n**Verify:** `cd mcp-server && python -m pytest tests/test_client_bridge.py -v` -> 2 passed\n\n```json:metadata\n{\"files\": [\"mcp-server/pyproject.toml\", \"mcp-server/dosbox_mcp/__init__.py\", \"mcp-server/dosbox_mcp/_client_import.py\", \"mcp-server/tests/__init__.py\", \"mcp-server/tests/test_client_bridge.py\"], \"verifyCommand\": \"cd mcp-server && python -m pytest tests/test_client_bridge.py -v\", \"acceptanceCriteria\": [\"pip install -e mcp-server/[test] succeeds\", \"_client_import re-exports match host_control_client objects exactly\"], \"modelTier\": \"mechanical\"}\n```",
      "activeForm": "Scaffolding mcp-server package and client bridge"
    },
    {
      "id": 1,
      "subject": "Task 2: Binary discovery",
      "status": "pending",
      "blockedBy": [0],
      "description": "**Goal:** Resolve a usable `dosbox-x` binary path: explicit override, then known install/build locations, then `PATH`.\n\n**Files:**\n- Create: `mcp-server/dosbox_mcp/binary_discovery.py`\n- Test: `mcp-server/tests/test_binary_discovery.py`\n\n**Acceptance Criteria:**\n- [ ] An explicit `binary_path` that exists is returned as-is; a nonexistent explicit path raises `BinaryNotFoundError`.\n- [ ] When no explicit path is given, a candidate location that exists on disk is returned.\n- [ ] When nothing matches on disk, falls back to `shutil.which(\"dosbox-x\")`.\n- [ ] When nothing is found anywhere, raises `BinaryNotFoundError` with a message telling the caller to pass `binary_path`.\n\n**Verify:** `cd mcp-server && python -m pytest tests/test_binary_discovery.py -v` -> 5 passed\n\n```json:metadata\n{\"files\": [\"mcp-server/dosbox_mcp/binary_discovery.py\", \"mcp-server/tests/test_binary_discovery.py\"], \"verifyCommand\": \"cd mcp-server && python -m pytest tests/test_binary_discovery.py -v\", \"acceptanceCriteria\": [\"explicit path honored or raises BinaryNotFoundError\", \"candidate path fallback works\", \"PATH fallback via shutil.which works\", \"raises when nothing found\"], \"modelTier\": \"mechanical\"}\n```",
      "activeForm": "Implementing dosbox-x binary discovery"
    },
    {
      "id": 2,
      "subject": "Task 3: SessionState container",
      "status": "pending",
      "blockedBy": [0],
      "description": "**Goal:** A thread-safe, pure in-memory holder for one session's observable state: phase, drive/cwd, accumulated output, last result, and the bad-command heuristic. No I/O.\n\n**Files:**\n- Create: `mcp-server/dosbox_mcp/session_state.py`\n- Test: `mcp-server/tests/test_session_state.py`\n\n**Acceptance Criteria:**\n- [ ] Starts in `STARTING`; a `ready` event moves it to `READY`.\n- [ ] `begin_request()` moves `READY`->`BUSY` and clears the previous result/bad-command flag.\n- [ ] A `result` (or `error`) event moves `BUSY`->`READY`, records drive/cwd, and makes `snapshot()[\"done\"]` true.\n- [ ] `drain_output()` returns only text accumulated since the last call, then clears it.\n- [ ] `snapshot()[\"bad_command\"]` becomes true only when an `output` event's decoded text contains `Bad command or filename`.\n- [ ] `snapshot()[\"done\"]` stays true across repeated calls until the next `begin_request()`.\n\n**Verify:** `cd mcp-server && python -m pytest tests/test_session_state.py -v` -> 7 passed\n\n```json:metadata\n{\"files\": [\"mcp-server/dosbox_mcp/session_state.py\", \"mcp-server/tests/test_session_state.py\"], \"verifyCommand\": \"cd mcp-server && python -m pytest tests/test_session_state.py -v\", \"acceptanceCriteria\": [\"ready event moves STARTING to READY\", \"begin_request moves READY to BUSY and clears prior result\", \"result/error event moves BUSY to READY and records drive/cwd\", \"drain_output returns only new text and clears buffer\", \"bad_command heuristic detects the DOS error string\", \"done stays true until next begin_request\"], \"modelTier\": \"mechanical\"}\n```",
      "activeForm": "Implementing thread-safe SessionState container"
    },
    {
      "id": 3,
      "subject": "Task 4: DosboxSession core lifecycle",
      "status": "pending",
      "blockedBy": [0, 1, 2],
      "description": "**Goal:** `DosboxSession`, the class owning a connected event source, a background reader thread, and the `exec`/`poll`/`send_input`/`status`/`stop` operations, enforcing the session state machine. Testable without a real DOSBox-X process via an injected fake event source.\n\n**Files:**\n- Create: `mcp-server/dosbox_mcp/session.py`\n- Test: `mcp-server/tests/fakes.py`\n- Test: `mcp-server/tests/test_session.py`\n\n**Acceptance Criteria:**\n- [ ] `exec()` raises `SessionError` unless the session is `READY`; succeeds and moves to `BUSY` when ready.\n- [ ] `poll()` blocks for at most `wait_seconds`, returns new output since the last poll, and reports `done`/`running`/`errorlevel`/`max_errorlevel`/`bad_command` from the current state.\n- [ ] `send_input()` raises `SessionError` unless `BUSY`; raises `SessionError` if given both or neither of `text`/`key`.\n- [ ] `stop()` sets phase to `STOPPED`, closes the event source, and joins the reader thread within a bounded timeout.\n- [ ] The reader thread transitions `STARTING`->`READY` on a `ready` event without racing any other reader of the event source (there is exactly one).\n- [ ] A closed/ended event source (`next_event` returns `None`) makes the reader thread mark the session `STOPPED` and exit.\n\n**Verify:** `cd mcp-server && python -m pytest tests/test_session.py -v` -> 9 passed\n\n```json:metadata\n{\"files\": [\"mcp-server/dosbox_mcp/session.py\", \"mcp-server/tests/fakes.py\", \"mcp-server/tests/test_session.py\"], \"verifyCommand\": \"cd mcp-server && python -m pytest tests/test_session.py -v\", \"acceptanceCriteria\": [\"exec requires READY, moves to BUSY\", \"poll bounded by wait_seconds and reports correct fields\", \"send_input requires BUSY and exactly one of text/key\", \"stop sets STOPPED, closes source, joins reader thread\", \"single-reader ready transition with no race\", \"closed event source marks session STOPPED\"], \"modelTier\": \"standard\"}\n```",
      "activeForm": "Implementing DosboxSession core lifecycle and reader thread"
    },
    {
      "id": 4,
      "subject": "Task 5: Setup-command replay coverage",
      "status": "pending",
      "blockedBy": [3],
      "description": "**Goal:** Verify and lock down `DosboxSession.launch()`'s setup-replay behavior -- the `mount`/`dos_path`/`env` params issued as real commands before `launch()` returns, since `[autoexec]` never runs under host control. The implementation (`_run_setup`/`_run_setup_command`) already exists from Task 4; this task is dedicated test coverage in isolation. No new production code should be needed; if these tests reveal a gap, fix `session.py` here.\n\n**Files:**\n- Test: `mcp-server/tests/test_session_setup.py`\n\n**Acceptance Criteria:**\n- [ ] With `mount` given, the setup sequence issues `mount <drive> \"<host_path>\"` then `<drive>:` before the caller-visible session is returned as ready-to-use.\n- [ ] `dos_path` (or the default `Z:\\;C:\\;C:\\TOOLS` when omitted) is issued as a `path` command.\n- [ ] Each `env` entry is issued as a `set KEY=value` command.\n- [ ] `setup_results` reports one entry per issued command with its `ok`/`errorlevel`.\n- [ ] A failing setup command (`ok: false`) still appears in `setup_results` rather than raising -- the caller decides whether to treat it as fatal.\n\n**Verify:** `cd mcp-server && python -m pytest tests/test_session_setup.py -v` -> 5 passed\n\n```json:metadata\n{\"files\": [\"mcp-server/tests/test_session_setup.py\"], \"verifyCommand\": \"cd mcp-server && python -m pytest tests/test_session_setup.py -v\", \"acceptanceCriteria\": [\"mount issues mount then drive-change command\", \"default dos_path used when omitted\", \"custom dos_path honored\", \"env vars issued as set commands\", \"setup_results reports one entry per command\"], \"modelTier\": \"mechanical\"}\n```",
      "activeForm": "Covering autoexec-replacement setup sequencing with tests"
    },
    {
      "id": 5,
      "subject": "Task 6: MCP server tool wiring",
      "status": "pending",
      "blockedBy": [3],
      "description": "**Goal:** `server.py` exposing the six tools via FastMCP, enforcing one-session-at-a-time, with a `main()` entry point for the `dosbox-mcp` console script.\n\n**Files:**\n- Create: `mcp-server/dosbox_mcp/server.py`\n- Test: `mcp-server/tests/test_server.py`\n\n**Acceptance Criteria:**\n- [ ] `start_session` fails with a clear error if a session is already active.\n- [ ] `start_session` returns `pid` and `setup_results` alongside session status.\n- [ ] `exec`, `poll`, `send_input`, `status` raise/return a clear error when no session is active (except `status`, which reports `session_active: false` rather than raising).\n- [ ] `stop_session` clears the active-session slot so a subsequent `start_session` succeeds.\n- [ ] The tool named `exec` does not shadow Python's builtin at the module level (implemented as `exec_command`, exposed as `exec` via the tool decorator's `name=` argument).\n\n**Verify:** `cd mcp-server && python -m pytest tests/test_server.py -v` -> 9 passed\n\n```json:metadata\n{\"files\": [\"mcp-server/dosbox_mcp/server.py\", \"mcp-server/tests/test_server.py\"], \"verifyCommand\": \"cd mcp-server && python -m pytest tests/test_server.py -v\", \"acceptanceCriteria\": [\"start_session rejects a second concurrent session\", \"start_session returns pid and setup_results\", \"exec/poll/send_input raise SessionError with no active session\", \"status returns session_active:false instead of raising\", \"stop_session clears the active session slot\", \"exec tool implemented as exec_command to avoid shadowing builtin\"], \"modelTier\": \"standard\"}\n```",
      "activeForm": "Wiring the six MCP tools onto DosboxSession"
    },
    {
      "id": 6,
      "subject": "Task 7: Opt-in live integration test",
      "status": "pending",
      "blockedBy": [5],
      "description": "**Goal:** One end-to-end test against a real DOSBox-X binary, gated behind an env var so it doesn't run by default (matching the main repo's `DOSBOX_X_LIVE_TESTS` pattern).\n\n**USER-ORDERED GATE -- NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:**\n- Create: `mcp-server/tests/test_live_integration.py`\n\n**Acceptance Criteria:**\n- [ ] Skipped by default; runs only when `DOSBOX_MCP_LIVE_TESTS=1` and `DOSBOX_X_BINARY` are both set.\n- [ ] Drives the real `server.py` tool functions directly against a real `DosboxSession.launch()` -- starts a session, runs a trivial DOS command (`ver`), polls to completion, asserts on output and errorlevel, then stops the session.\n\n**Verify:** `cd mcp-server && DOSBOX_MCP_LIVE_TESTS=1 DOSBOX_X_BINARY=\"<path>\" python -m pytest tests/test_live_integration.py -v` -> 1 passed; and `cd mcp-server && python -m pytest tests/test_live_integration.py -v` -> 1 skipped by default\n\n```json:metadata\n{\"files\": [\"mcp-server/tests/test_live_integration.py\"], \"verifyCommand\": \"cd mcp-server && DOSBOX_MCP_LIVE_TESTS=1 DOSBOX_X_BINARY=\\\"../bin/x64/Release SDL2/dosbox-x.exe\\\" python -m pytest tests/test_live_integration.py -v\", \"acceptanceCriteria\": [\"skipped by default without env vars\", \"real start_session/exec/poll/stop_session round trip passes against a real binary\", \"asserts errorlevel 0 and non-empty output from a trivial DOS command\"], \"modelTier\": \"standard\", \"userGate\": true, \"tags\": [\"user-gate\"]}\n```",
      "activeForm": "Adding opt-in live integration test against a real binary"
    },
    {
      "id": 7,
      "subject": "Task 8: README and MCP client config example",
      "status": "pending",
      "blockedBy": [5],
      "description": "**Goal:** A short README covering install, the tool contract, and how to register the server in an MCP-compatible agent host, so this is actually usable -- not just importable.\n\n**Files:**\n- Create: `mcp-server/README.md`\n\n**Acceptance Criteria:**\n- [ ] Documents install (`pip install -e mcp-server/`), the `dosbox-mcp` entry point, and an example MCP client config JSON block.\n- [ ] Documents all six tools with their parameters, in one table.\n- [ ] States the two workarounds (autoexec replay, bad-command scan) and the one omission (no `cancel` tool -- `stop_session` is the abort path) plainly.\n\n**Verify:** Manual read-through; no automated check for a README.\n\n```json:metadata\n{\"files\": [\"mcp-server/README.md\"], \"verifyCommand\": \"\", \"acceptanceCriteria\": [\"install instructions present\", \"MCP client config example present\", \"all six tools documented in a table\", \"known limitations (autoexec, bad_command heuristic, no cancel, one session at a time) documented\"], \"modelTier\": \"mechanical\"}\n```",
      "activeForm": "Writing README and MCP client configuration example"
    }
  ],
  "lastUpdated": "2026-08-16T00:00:00Z"
}
